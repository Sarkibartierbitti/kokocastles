import ComingSoon from '~/app/components/ComingSoon';

export default function MyChannel() {
  return (
    <ComingSoon
      kind="my channel"
      phase={6}
      description="Analytics over your own uploads, with hypothesis tagging and 6-hour polling refresh."
    />
  );
}
